# Podman 安装

# Debian

```bash
sudo apt install podman podman-compose -y
```

# Windows

1. 安装 podman cli

2. 执行初始化命令

   ```bash
   podman machine init
   podman machine start
   ```

3. 安装 podman desktop
